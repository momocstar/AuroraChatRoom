syntax = "proto3"; // proto3 必须加此注解

option java_package = "com.momoc.multi.chat.room.common.proto"; // 生成类的包名，注意：会在指定路径下按照该包名的定义来生成文件夹
// 消息模型定义

message sessionBuildResponse_100{
  string token = 1;
}

//心跳消息
message heartPopMessage_200{

}

//用户未进行登录注册
message UserNotRegisterChannel_201{

}